// identier Rule


// all javaScript variable must be identified with unique name(identifers).

// name can contain letters, digits , underscore, and dollar sign . (no space )

// name must begin with a letter

// name can also begin with $ and _.

// name are case sensitive(y and Y are different variables).

// reserved words (like Javascript keywords ) cannot be used as names.





// camelCase
// way of writing identifiers

// camelCase (js, c++, java naming convention .-> not a rule)


// snake_case (python)

// PascalCase
