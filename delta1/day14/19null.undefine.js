// Null and Undefined in js
// undefined
// A variable that has not been assigned a value is of type undefined.
// if you try to print value that not know js about its value

// as 
let a;
console.log(a);
let b = typeof a;
console.log(b); //undefine


// null
// the null vaule represents the intentional (qsdan )absence of any object value.

// to be explicitly assigned.
// null is a keyword as other var let


let c = null;  // if not fil the value with any value we intialize with null value  if give value leter on 
console.log(c);
let d = typeof c;
console.log(d); //object