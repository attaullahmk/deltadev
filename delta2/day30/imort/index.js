import { sum, Pi } from "./math1.js";
// alwys write full name of file with extention not name of file as in require function
// first write in json file if use import method 
// "type": "module"
console.log(sum(2, 4));
console.log(Pi);
